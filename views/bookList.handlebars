<div class="mb-6 border-b border-stone-800 pb-6">
  <div class="mb-4">
    <a href="/books/new" class="p-2 bg-stone-600 hover:bg-opacity-70 active:scale-95 rounded-md mr-2 mb-4">Новая книга</a>
  </div>
  <div>
    <div class="mb-4">{{> titleSearch }}</div>
    <div class="buttons flex mb-4">
      <div class="mr-2">
        <form action="?">
          <button type="submit" class="px-2 py-1 bg-stone-600">Сбросить фильтры</button>
        </form>
      </div>
    </div>
  </div>
</div>
{{#each books}}
  {{> bookCard book=this haveLink=true}}
{{/each}}